# A sample Guardfile
# More info at https://github.com/guard/guard#readme
guard 'jekyll' do
  watch /.*/
  ignore /public/
end

guard 'livereload' do
  watch /.*/
end

guard 'passenger' do
  watch /humans.txt/
end
